name: Release Build

on:
  push:
    tags:
      - '**'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Print Tag
        run: |
          TAG="${{ github.ref_name }}"
          echo "${TAG/ciao/uno}" 
